import React from 'react';

export default function Committing() {
  return (
    <div
      style={{
        position: 'absolute',
        top: 0,
        left: 0,
        width: '100%',
        height: '100%',
        display: 'flex',
        alignItems: 'center',
        justifyContent: 'center',
        background: 'rgba(0, 0, 0, 0.5)',
        fontSize: 36,
        color: '#FFFFFF',
      }}
    >
      Pretending to save...
    </div>
  );
}
